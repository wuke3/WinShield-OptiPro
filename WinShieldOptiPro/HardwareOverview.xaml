<UserControl x:Class="WinShieldOptiPro.HardwareOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="#121212">
        <!-- 标题 -->
        <TextBlock Text="硬件配置概览" FontSize="24" FontWeight="Bold" Foreground="#00BFFF" Margin="20,20,0,0"/>
        
        <!-- 操作按钮 -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,20,0">
            <Button x:Name="CopyButton" Content="一键复制" Width="100" Height="30" Background="#00BFFF" Foreground="#FFFFFF" BorderThickness="0" Margin="0,0,10,0" Click="CopyButton_Click"/>
            <Button x:Name="ExportButton" Content="导出文本" Width="100" Height="30" Background="#00BFFF" Foreground="#FFFFFF" BorderThickness="0" Click="ExportButton_Click"/>
        </StackPanel>
        
        <!-- 核心硬件部分 -->
        <Border Margin="20,60,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="核心硬件信息" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                
                <!-- CPU信息 -->
                <Border Margin="0,40,0,0" Background="#2A2A2A" CornerRadius="6" Padding="15">
                    <Grid>
                        <TextBlock Text="处理器 (CPU)" FontSize="14" FontWeight="Bold" Foreground="#00BFFF"/>
                        <TextBlock x:Name="CPUInfoText" Text="正在获取..." FontSize="12" Foreground="#FFFFFF" Margin="0,25,0,0"/>
                    </Grid>
                </Border>
                
                <!-- 内存信息 -->
                <Border Margin="0,120,0,0" Background="#2A2A2A" CornerRadius="6" Padding="15">
                    <Grid>
                        <TextBlock Text="内存条 (RAM)" FontSize="14" FontWeight="Bold" Foreground="#00BFFF"/>
                        <TextBlock x:Name="RAMInfoText" Text="正在获取..." FontSize="12" Foreground="#FFFFFF" Margin="0,25,0,0"/>
                    </Grid>
                </Border>
                
                <!-- 显卡信息 -->
                <Border Margin="0,200,0,0" Background="#2A2A2A" CornerRadius="6" Padding="15">
                    <Grid>
                        <TextBlock Text="显卡 (GPU)" FontSize="14" FontWeight="Bold" Foreground="#00BFFF"/>
                        <TextBlock x:Name="GPUInfoText" Text="正在获取..." FontSize="12" Foreground="#FFFFFF" Margin="0,25,0,0"/>
                    </Grid>
                </Border>
                
                <!-- 主板信息 -->
                <Border Margin="0,280,0,0" Background="#2A2A2A" CornerRadius="6" Padding="15">
                    <Grid>
                        <TextBlock Text="主板" FontSize="14" FontWeight="Bold" Foreground="#00BFFF"/>
                        <TextBlock x:Name="MotherboardInfoText" Text="正在获取..." FontSize="12" Foreground="#FFFFFF" Margin="0,25,0,0"/>
                    </Grid>
                </Border>
                
                <!-- 声卡和网卡信息 -->
                <Border Margin="0,360,0,0" Background="#2A2A2A" CornerRadius="6" Padding="15">
                    <Grid>
                        <TextBlock Text="声卡 &amp; 网卡" FontSize="14" FontWeight="Bold" Foreground="#00BFFF"/>
                        <TextBlock x:Name="AudioNetworkInfoText" Text="正在获取..." FontSize="12" Foreground="#FFFFFF" Margin="0,25,0,0"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
        
        <!-- 外设硬件部分 -->
        <Border Margin="20,460,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="外设硬件信息" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                
                <Grid Margin="0,40,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- 显示器信息 -->
                    <Border Grid.Column="0" Background="#2A2A2A" CornerRadius="6" Padding="15" Margin="0,0,10,0">
                        <Grid>
                            <TextBlock Text="显示器" FontSize="14" FontWeight="Bold" Foreground="#00BFFF"/>
                            <TextBlock x:Name="MonitorInfoText" Text="正在获取..." FontSize="12" Foreground="#FFFFFF" Margin="0,25,0,0"/>
                        </Grid>
                    </Border>
                    
                    <!-- 硬盘信息 -->
                    <Border Grid.Column="1" Background="#2A2A2A" CornerRadius="6" Padding="15" Margin="10,0,0,0">
                        <Grid>
                            <TextBlock Text="硬盘" FontSize="14" FontWeight="Bold" Foreground="#00BFFF"/>
                            <TextBlock x:Name="DiskInfoText" Text="正在获取..." FontSize="12" Foreground="#FFFFFF" Margin="0,25,0,0"/>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Border>
        
        <!-- 系统信息部分 -->
        <Border Margin="20,560,20,40" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="系统信息" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                
                <TextBlock x:Name="SystemInfoText" Text="正在获取..." FontSize="12" Foreground="#FFFFFF" Margin="0,40,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>