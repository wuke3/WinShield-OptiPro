<UserControl x:Class="WinShieldOptiPro.PopupBlocker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="#0078D7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                                <Setter Property="BorderBrush" Value="#00A4EF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004785"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGrid" x:Key="CustomDataGrid">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#252525"/>
            <Setter Property="AlternatingRowBackground" Value="#2A2A2A"/>
            <Setter Property="VerticalGridLinesBrush" Value="#333333"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#333333"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#2A2A2A"/>
                        <Setter Property="Foreground" Value="#00A4EF"/>
                        <Setter Property="BorderBrush" Value="#333333"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <!-- 背景渐变 -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0A0A0A" Offset="0"/>
                <GradientStop Color="#1A1A1A" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        
        <!-- 毛玻璃效果 -->
        <Border BorderThickness="1" BorderBrush="#333333" CornerRadius="8" Margin="2">
            <Border.Background>
                <SolidColorBrush Color="#1E1E1E" Opacity="0.8"/>
            </Border.Background>
        </Border>
        
        <!-- 主内容 -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 顶部标题栏 -->
            <Border Grid.Row="0" Background="#252525" BorderBrush="#0078D7" BorderThickness="0 0 0 1" CornerRadius="4 4 0 0">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0">
                        <TextBlock Text="弹窗拦截管家" FontSize="18" FontWeight="Bold" Foreground="#00A4EF">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#00A4EF" BlurRadius="10" Opacity="0.5"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock x:Name="StatusText" Text="(拦截中)" FontSize="14" Foreground="#4CAF50" Margin="10,0,0,0">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#4CAF50" BlurRadius="5" Opacity="0.5"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- 主要内容区 -->
            <Grid Grid.Row="1" Margin="0,10,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="100"/>
                </Grid.RowDefinitions>
                
                <!-- 控制按钮 -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Content="启动拦截" Style="{StaticResource PrimaryButton}" Click="StartBlocking"/>
                    <Button Content="停止拦截" Style="{StaticResource PrimaryButton}" Click="StopBlocking"/>
                    <Button Content="添加黑名单" Style="{StaticResource PrimaryButton}" Click="AddToBlacklist"/>
                    <Button Content="添加白名单" Style="{StaticResource PrimaryButton}" Click="AddToWhitelist"/>
                    <Button Content="管理规则" Style="{StaticResource PrimaryButton}" Click="ManageRules"/>
                    <Button Content="查看日志" Style="{StaticResource PrimaryButton}" Click="ViewLogs"/>
                </StackPanel>
                
                <!-- 拦截日志 -->
                <Border Grid.Row="1" BorderThickness="1" BorderBrush="#333333" CornerRadius="4" Background="#1A1A1A">
                    <DataGrid x:Name="LogDataGrid" Style="{StaticResource CustomDataGrid}" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="150"/>
                            <DataGridTextColumn Header="弹窗类型" Binding="{Binding Type}" Width="120"/>
                            <DataGridTextColumn Header="进程名称" Binding="{Binding ProcessName}" Width="150"/>
                            <DataGridTextColumn Header="窗口标题" Binding="{Binding WindowTitle}" Width="*"/>
                            <DataGridTextColumn Header="操作" Binding="{Binding Action}" Width="100"/>
                            <DataGridTemplateColumn Header="操作" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="定位软件" Click="LocateSoftware" Tag="{Binding ProcessName}" Background="#2A2A2A" Foreground="#00A4EF" BorderBrush="#333333"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
                
                <!-- 统计信息 -->
                <Border Grid.Row="2" Background="#252525" CornerRadius="4" Margin="0,10,0,0" BorderBrush="#333333" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="20" HorizontalAlignment="Center">
                            <TextBlock Text="今日拦截" FontSize="14" Foreground="#E0E0E0" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TodayCount" Text="0" FontSize="24" FontWeight="Bold" Foreground="#00A4EF" HorizontalAlignment="Center">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="#00A4EF" BlurRadius="5" Opacity="0.5"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="20" HorizontalAlignment="Center">
                            <TextBlock Text="本周拦截" FontSize="14" Foreground="#E0E0E0" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="WeekCount" Text="0" FontSize="24" FontWeight="Bold" Foreground="#00A4EF" HorizontalAlignment="Center">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="#00A4EF" BlurRadius="5" Opacity="0.5"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="20" HorizontalAlignment="Center">
                            <TextBlock Text="黑名单" FontSize="14" Foreground="#E0E0E0" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="BlacklistCount" Text="0" FontSize="24" FontWeight="Bold" Foreground="#00A4EF" HorizontalAlignment="Center">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="#00A4EF" BlurRadius="5" Opacity="0.5"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Grid.Column="3" Margin="20" HorizontalAlignment="Center">
                            <TextBlock Text="白名单" FontSize="14" Foreground="#E0E0E0" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="WhitelistCount" Text="0" FontSize="24" FontWeight="Bold" Foreground="#00A4EF" HorizontalAlignment="Center">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="#00A4EF" BlurRadius="5" Opacity="0.5"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>