<UserControl x:Class="WinShieldOptiPro.SystemScanner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="#121212" CornerRadius="10" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- 标题区域 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,20,20,10" VerticalAlignment="Center">
                <TextBlock Text="系统静默查杀" FontSize="24" FontWeight="Bold" Foreground="#00BFFF"/>
                <TextBlock Text=" - 基于Windows Defender" FontSize="16" Foreground="#888" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- 主要内容区域 -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- 扫描模式选择 -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                    <RadioButton x:Name="QuickScanRadio" Content="快速扫描" GroupName="ScanMode" IsChecked="True" Foreground="#DDD" Margin="0,0,20,0"/>
                    <RadioButton x:Name="FullScanRadio" Content="全盘扫描" GroupName="ScanMode" Foreground="#DDD" Margin="0,0,20,0"/>
                    <RadioButton x:Name="CustomScanRadio" Content="自定义目录" GroupName="ScanMode" Foreground="#DDD"/>
                    <Button x:Name="BrowseButton" Content="浏览" Foreground="#DDD" Background="#333" BorderBrush="#555" Margin="10,0,0,0" Visibility="Collapsed" Click="BrowseButton_Click"/>
                    <TextBlock x:Name="CustomPathText" Text="未选择目录" Foreground="#888" Margin="10,0,0,0" Visibility="Collapsed"/>
                </StackPanel>
                
                <!-- 2D防护盾动画区域 -->
                <Grid Grid.Row="1" Margin="0,20,0,20">
                    <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Grid Width="300" Height="300">
                            <!-- 背景圆环 -->
                            <Ellipse Width="280" Height="280" Fill="#001122" Stroke="#00BFFF" StrokeThickness="2" Opacity="0.3">
                                <Ellipse.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Ellipse.Opacity)" From="0.2" To="0.4" Duration="0:0:3" RepeatBehavior="Forever" AutoReverse="True"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Ellipse.Triggers>
                            </Ellipse>
                            
                            <!-- 中间圆环 -->
                            <Ellipse Width="240" Height="240" Fill="#001A33" Stroke="#00BFFF" StrokeThickness="1" Opacity="0.5">
                                <Ellipse.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Ellipse.Opacity)" From="0.4" To="0.6" Duration="0:0:2.5" RepeatBehavior="Forever" AutoReverse="True"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Ellipse.Triggers>
                            </Ellipse>
                            
                            <!-- 内部圆环 -->
                            <Ellipse Width="200" Height="200" Fill="#002244" Stroke="#00BFFF" StrokeThickness="1" Opacity="0.7">
                                <Ellipse.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Ellipse.Opacity)" From="0.6" To="0.8" Duration="0:0:2" RepeatBehavior="Forever" AutoReverse="True"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Ellipse.Triggers>
                            </Ellipse>
                            
                            <!-- 扫描状态文本 -->
                            <TextBlock x:Name="ShieldStatus" Text="空闲" FontSize="20" FontWeight="Bold" Foreground="#00BFFF" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(TextBlock.Opacity)" From="0.8" To="1" Duration="0:0:1.5" RepeatBehavior="Forever" AutoReverse="True"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </TextBlock.Triggers>
                            </TextBlock>
                            
                            <!-- 扫描进度条 -->
                            <ProgressBar x:Name="ScanProgress" Width="200" Height="10" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,40" Minimum="0" Maximum="100" Value="0" Visibility="Collapsed">
                                <ProgressBar.Foreground>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#00BFFF" Offset="0"/>
                                        <GradientStop Color="#007FFF" Offset="1"/>
                                    </LinearGradientBrush>
                                </ProgressBar.Foreground>
                            </ProgressBar>
                            
                            <!-- 进度百分比文本 -->
                            <TextBlock x:Name="ProgressText" Text="0%" Foreground="#DDD" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20" Visibility="Collapsed"/>
                        </Grid>
                    </Viewbox>
                </Grid>
                
                <!-- 扫描结果区域 -->
                <StackPanel Grid.Row="2" Margin="0,20,0,0">
                    <TextBlock x:Name="ScanResult" Text="准备就绪" FontSize="16" Foreground="#DDD" Margin="0,0,0,10"/>
                    <ListBox x:Name="ThreatList" Height="150" Background="#1E1E1E" BorderBrush="#333" Foreground="#DDD">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="●" Foreground="#FF4444" Margin="0,0,10,0"/>
                                    <TextBlock Text="{Binding Path}" Foreground="#DDD"/>
                                    <TextBlock Text=" - " Foreground="#888" Margin="5,0,5,0"/>
                                    <TextBlock Text="{Binding Type}" Foreground="#888"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Grid>
            
            <!-- 按钮区域 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="20" HorizontalAlignment="Center">
                <Button x:Name="StartScanButton" Content="开始扫描" Width="120" Height="40" Background="#00BFFF" Foreground="#121212" FontWeight="Bold" Click="StartScanButton_Click"/>
                <Button x:Name="StopScanButton" Content="停止扫描" Width="120" Height="40" Background="#FF4444" Foreground="White" FontWeight="Bold" Margin="20,0,0,0" Click="StopScanButton_Click" IsEnabled="False"/>
                <Button x:Name="ClearResultsButton" Content="清除结果" Width="120" Height="40" Background="#333" Foreground="#DDD" Margin="20,0,0,0" Click="ClearResultsButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>