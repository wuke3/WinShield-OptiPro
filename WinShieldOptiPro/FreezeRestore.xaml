<UserControl x:Class="WinShieldOptiPro.FreezeRestore"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="#121212">
        <!-- 标题 -->
        <TextBlock Text="自定义冰点还原" FontSize="24" FontWeight="Bold" Foreground="#00BFFF" Margin="20,20,0,0"/>
        
        <!-- 保护分区自定义 -->
        <Border Margin="20,60,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="保护分区自定义" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                <StackPanel Margin="0,40,0,0">
                    <CheckBox x:Name="CDriveCheckBox" Content="C盘（系统盘）" Margin="0,5,0,0" Foreground="#FFFFFF" IsChecked="True"/>
                    <CheckBox x:Name="DDriveCheckBox" Content="D盘" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="EDriveCheckBox" Content="E盘" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="FDriveCheckBox" Content="F盘" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 还原模式自由切换 -->
        <Border Margin="20,200,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="还原模式" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                <StackPanel Margin="0,40,0,0">
                    <RadioButton x:Name="RestartRestoreRadio" Content="重启即时还原" Margin="0,5,0,0" Foreground="#FFFFFF" IsChecked="True"/>
                    <RadioButton x:Name="ScheduledRestoreRadio" Content="定时周期还原" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <StackPanel Orientation="Horizontal" Margin="20,5,0,0" Visibility="Collapsed" x:Name="ScheduledRestorePanel">
                        <ComboBox x:Name="IntervalComboBox" Width="120" Height="30" Background="#2A2A2A" Foreground="#FFFFFF" Margin="0,0,10,0">
                            <ComboBoxItem Content="每日"/>
                            <ComboBoxItem Content="每周"/>
                            <ComboBoxItem Content="每月"/>
                        </ComboBox>
                        <DatePicker x:Name="RestoreTimePicker" Width="120" Height="30" Background="#2A2A2A" Foreground="#FFFFFF"/>
                    </StackPanel>
                    <RadioButton x:Name="ManualRestoreRadio" Content="手动触发还原" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 免还原白名单 -->
        <Border Margin="20,340,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="免还原白名单" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                <StackPanel Margin="0,40,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="WhitelistPathTextBox" Grid.Column="0" Height="30" Background="#2A2A2A" Foreground="#FFFFFF" BorderThickness="1" BorderBrush="#3A3A3A" Margin="0,0,10,0"/>
                        <Button x:Name="BrowseButton" Content="浏览" Grid.Column="1" Height="30" Background="#00BFFF" Foreground="#FFFFFF" Margin="0,0,10,0"/>
                        <Button x:Name="AddToWhitelistButton" Content="添加" Grid.Column="2" Height="30" Background="#00BFFF" Foreground="#FFFFFF"/>
                    </Grid>
                    <ListBox x:Name="WhitelistListBox" Margin="0,10,0,0" Background="#2A2A2A" Foreground="#FFFFFF" Height="80">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding}" Width="800"/>
                                    <Button Content="删除" Width="60" Height="20" Background="#FF4500" Foreground="#FFFFFF" Margin="10,0,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 防护时效自定义 -->
        <Border Margin="20,480,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="防护时效" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                <StackPanel Margin="0,40,0,0">
                    <RadioButton x:Name="AlwaysProtectRadio" Content="始终开启保护" Margin="0,5,0,0" Foreground="#FFFFFF" IsChecked="True"/>
                    <RadioButton x:Name="TemporaryDisableRadio" Content="临时关闭保护" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <StackPanel Orientation="Horizontal" Margin="20,5,0,0" Visibility="Collapsed" x:Name="TemporaryDisablePanel">
                        <ComboBox x:Name="DisableDurationComboBox" Width="120" Height="30" Background="#2A2A2A" Foreground="#FFFFFF" Margin="0,0,10,0">
                            <ComboBoxItem Content="1小时"/>
                            <ComboBoxItem Content="4小时"/>
                            <ComboBoxItem Content="半天"/>
                            <ComboBoxItem Content="全天"/>
                        </ComboBox>
                        <TextBlock Text="后自动重启防护" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 自定义系统快照 -->
        <Border Margin="20,620,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="自定义系统快照" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                <StackPanel Margin="0,40,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="SnapshotNameTextBox" Grid.Column="0" Height="30" Background="#2A2A2A" Foreground="#FFFFFF" BorderThickness="1" BorderBrush="#3A3A3A" Margin="0,0,10,0"/>
                        <Button x:Name="CreateSnapshotButton" Content="创建快照" Grid.Column="1" Height="30" Background="#00BFFF" Foreground="#FFFFFF"/>
                    </Grid>
                    <ListBox x:Name="SnapshotsListBox" Margin="0,10,0,0" Background="#2A2A2A" Foreground="#FFFFFF" Height="120">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" Width="400"/>
                                    <TextBlock Text="{Binding Time}" Width="200"/>
                                    <Button Content="还原" Width="60" Height="20" Background="#00BFFF" Foreground="#FFFFFF" Margin="10,0,0,0"/>
                                    <Button Content="删除" Width="60" Height="20" Background="#FF4500" Foreground="#FFFFFF" Margin="10,0,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 操作按钮 -->
        <Grid Margin="20,800,20,40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="EnableProtectionButton" Content="开启保护" Width="150" Height="50" Background="#00BFFF" Foreground="#FFFFFF" FontSize="14" FontWeight="Bold" Grid.Column="0" Margin="0,0,10,0"/>
            <Button x:Name="DisableProtectionButton" Content="关闭保护" Width="150" Height="50" Background="#FF4500" Foreground="#FFFFFF" FontSize="14" FontWeight="Bold" Grid.Column="1" Margin="10,0,10,0"/>
            <Button x:Name="ApplySettingsButton" Content="应用设置" Width="150" Height="50" Background="#00BFFF" Foreground="#FFFFFF" FontSize="14" FontWeight="Bold" Grid.Column="2" Margin="10,0,0,0"/>
        </Grid>
    </Grid>
</UserControl>