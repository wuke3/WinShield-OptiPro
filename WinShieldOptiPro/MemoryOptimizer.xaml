<UserControl x:Class="WinShieldOptiPro.MemoryOptimizer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="{StaticResource SecondaryBackground}" BorderThickness="0 0 0 1" BorderBrush="{StaticResource AccentColor}">
            <StackPanel Orientation="Horizontal" Margin="20,10,20,10">
                <TextBlock Text="内存智能优化" Style="{StaticResource SubtitleText}"/>
                <TextBlock Text=" - 释放闲置内存，提升系统流畅度" Style="{StaticResource CaptionText}" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- 主内容区 -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 内存状态卡片 -->
            <Border Grid.Row="0" Style="{StaticResource CardBorder}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock Text="总内存" Style="{StaticResource CaptionText}"/>
                        <TextBlock x:Name="TotalMemoryText" Text="0 GB" Style="{StaticResource BodyText}" FontWeight="Bold" Foreground="{StaticResource AccentColor}" Margin="0,5,0,0"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                        <TextBlock Text="已用内存" Style="{StaticResource CaptionText}"/>
                        <TextBlock x:Name="UsedMemoryText" Text="0 GB" Style="{StaticResource BodyText}" FontWeight="Bold" Foreground="{StaticResource WarningColor}" Margin="0,5,0,0"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <TextBlock Text="可用内存" Style="{StaticResource CaptionText}"/>
                        <TextBlock x:Name="AvailableMemoryText" Text="0 GB" Style="{StaticResource BodyText}" FontWeight="Bold" Foreground="{StaticResource SuccessColor}" Margin="0,5,0,0"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                        <TextBlock Text="可释放内存" Style="{StaticResource CaptionText}"/>
                        <TextBlock x:Name="FreedMemoryText" Text="0 GB" Style="{StaticResource BodyText}" FontWeight="Bold" Foreground="#FFD166" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- 内存使用进度条 -->
            <Border Grid.Row="1" Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="内存使用情况" Style="{StaticResource CaptionText}" Margin="0,0,0,10"/>
                    <ProgressBar x:Name="MemoryUsageBar" Style="{StaticResource CustomProgressBar}" Height="20"/>
                    <TextBlock x:Name="MemoryUsageText" Text="0%" Style="{StaticResource CaptionText}" HorizontalAlignment="Right" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- 操作按钮区 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,20" HorizontalAlignment="Left">
                <Button x:Name="OptimizeButton" Content="一键内存加速" Style="{StaticResource PrimaryButton}"/>
                <Button x:Name="BackgroundOptimizeButton" Content="后台智能优化" Style="{StaticResource SecondaryButton}" Background="{StaticResource SuccessColor}"/>
                <Button x:Name="ProcessOptimizeButton" Content="进程内存管控" Style="{StaticResource SecondaryButton}" Background="#FFD166"/>
                <ToggleButton x:Name="AutoOptimizeToggle" Content="自动优化" Style="{StaticResource CustomToggleButton}" Width="120" Height="40"/>
            </StackPanel>
            
            <!-- 进程列表 -->
            <Border Grid.Row="3" Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="进程内存占用" Style="{StaticResource CaptionText}" Margin="0,0,0,10"/>
                    <DataGrid x:Name="ProcessList" AutoGenerateColumns="False" Style="{StaticResource CustomDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="进程名称" Binding="{Binding ProcessName}" Width="*"/>
                            <DataGridTextColumn Header="内存占用" Binding="{Binding MemoryUsage}" Width="120"/>
                            <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
        
        <!-- 底部状态栏 -->
        <Border Grid.Row="2" Background="{StaticResource SecondaryBackground}" BorderThickness="1 0 0 0" BorderBrush="{StaticResource AccentColor}">
            <StackPanel Orientation="Horizontal" Margin="20,10,20,10" HorizontalAlignment="Right">
                <TextBlock x:Name="StatusText" Text="就绪" Style="{StaticResource BodyText}" Foreground="{StaticResource SuccessColor}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>