<UserControl x:Class="WinShieldOptiPro.SystemOptimizer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="#121212">
        <!-- 标题 -->
        <TextBlock Text="智能全维度系统优化" FontSize="24" FontWeight="Bold" Foreground="#00BFFF" Margin="20,20,0,0"/>
        
        <!-- 存储瘦身优化 -->
        <Border Margin="20,60,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="存储瘦身优化" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                
                <StackPanel Margin="0,40,0,0">
                    <CheckBox x:Name="StorageSenseCheckBox" Content="开启存储感知" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="AutoCleanCheckBox" Content="自动清理临时文件/回收站/过期下载" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <CheckBox x:Name="CustomCleanPeriodCheckBox" Content="自定义自动清理周期" Margin="0,0,10,0" Foreground="#FFFFFF"/>
                        <ComboBox x:Name="CleanPeriodComboBox" Width="150" Background="#2A2A2A" Foreground="#FFFFFF" BorderBrush="#444444">
                            <ComboBoxItem Content="每天"/>
                            <ComboBoxItem Content="每周"/>
                            <ComboBoxItem Content="每月"/>
                        </ComboBox>
                    </StackPanel>
                    <CheckBox x:Name="MoveDefaultPathCheckBox" Content="一键迁移系统默认路径至非C盘" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="OptimizeHibernationCheckBox" Content="智能适配硬件优化休眠" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="OptimizeVirtualMemoryCheckBox" Content="智能适配硬件优化虚拟内存" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="LimitRestorePointCheckBox" Content="限制还原点占用" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="DisableReservedStorageCheckBox" Content="关闭系统预留存储" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 网络专项提速优化 -->
        <Border Margin="20,240,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="网络专项提速优化" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                
                <StackPanel Margin="0,40,0,0">
                    <CheckBox x:Name="OptimizePacketStrategyCheckBox" Content="优化系统默认分组报文策略" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="EnableFastForwardCheckBox" Content="开启系统网络快速转发机制" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="FixNetworkLinkCheckBox" Content="修复网络链路参数" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="OptimizeTCPConfigCheckBox" Content="优化TCP传输配置" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 系统深层性能优化 -->
        <Border Margin="20,360,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="系统深层性能优化" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                
                <StackPanel Margin="0,40,0,0">
                    <CheckBox x:Name="DisableDebugCheckBox" Content="关闭系统无用自动调试功能" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="DisableRedundantProcessesCheckBox" Content="关停后台冗余系统进程和闲置系统服务" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="OptimizeKernelSchedulerCheckBox" Content="优化系统内核调度参数" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="ReduceLogWritingCheckBox" Content="精简系统后台日志写入" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 系统稳定性优化 -->
        <Border Margin="20,480,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="系统稳定性优化" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                
                <StackPanel Margin="0,40,0,0">
                    <CheckBox x:Name="FixConfigAbnormalCheckBox" Content="修复系统隐性配置异常" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="OptimizeCrashReportCheckBox" Content="优化系统崩溃上报机制" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="ControlBackgroundResourcesCheckBox" Content="管控系统后台资源占用" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="OptimizeDriverConfigCheckBox" Content="优化系统驱动关联配置" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 用户界面与运行速度优化 -->
        <Border Margin="20,600,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="用户界面与运行速度优化" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                
                <StackPanel Margin="0,40,0,0">
                    <CheckBox x:Name="DisableRedundantAnimationsCheckBox" Content="关闭冗余视觉动画" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="OptimizeWindowRenderingCheckBox" Content="优化桌面窗口渲染速度" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="DisableBackgroundProcessesCheckBox" Content="关停后台无用悬浮进程" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 系统杂项精细化配置 -->
        <Border Margin="20,700,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="系统杂项精细化配置" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                
                <StackPanel Margin="0,40,0,0">
                    <CheckBox x:Name="ShowFileExtensionsCheckBox" Content="显示文件扩展名" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="ShowSystemVersionCheckBox" Content="在桌面显示系统版本信息" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="HideShutdownButtonCheckBox" Content="锁屏界面隐藏关机按钮" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="OptimizeFileAssociationsCheckBox" Content="优化文件关联配置" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="CleanInvalidContextMenuCheckBox" Content="清理无效系统右键菜单" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 隐私基础优化 -->
        <Border Margin="20,820,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="隐私基础优化" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                
                <StackPanel Margin="0,40,0,0">
                    <CheckBox x:Name="DisableSystemAdsCheckBox" Content="关闭系统广告" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="DisableDiagnosticDataCheckBox" Content="关闭诊断数据" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                    <CheckBox x:Name="CleanUsageTracesCheckBox" Content="清理使用痕迹" Margin="0,5,0,0" Foreground="#FFFFFF"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 自启动管理 -->
        <Border Margin="20,900,20,0" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="自启动管理" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                
                <StackPanel Margin="0,40,0,0">
                    <Button x:Name="ScanStartupItemsButton" Content="扫描自启项" Width="150" Height="40" Background="#00BFFF" Foreground="#FFFFFF" FontSize="14" FontWeight="Bold" Margin="0,0,0,10" Click="ScanStartupItemsButton_Click"/>
                    
                    <DataGrid x:Name="StartupItemsDataGrid" AutoGenerateColumns="False" Background="#121212" Foreground="#FFFFFF" BorderThickness="0" Height="200" Margin="0,10,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="120"/>
                            <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100"/>
                            <DataGridTextColumn Header="建议" Binding="{Binding Recommendation}" Width="120"/>
                            <DataGridTemplateColumn Header="操作" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button x:Name="EnableButton" Content="启用" Width="80" Height="25" Background="#2A2A2A" Foreground="#FFFFFF" BorderThickness="0" Click="EnableButton_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <Button x:Name="DisableRedundantStartupButton" Content="一键禁用冗余自启项" Width="200" Height="40" Background="#00BFFF" Foreground="#FFFFFF" FontSize="14" FontWeight="Bold" Margin="0,10,0,0" Click="DisableRedundantStartupButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 操作按钮 -->
        <Grid Margin="20,1180,20,40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="AnalyzeButton" Content="分析系统" Width="150" Height="50" Background="#00BFFF" Foreground="#FFFFFF" FontSize="14" FontWeight="Bold" Grid.Column="0" Margin="0,0,10,0" Click="AnalyzeButton_Click"/>
            <Button x:Name="OptimizeButton" Content="开始优化" Width="150" Height="50" Background="#00BFFF" Foreground="#FFFFFF" FontSize="14" FontWeight="Bold" Grid.Column="1" Margin="10,0,10,0" Click="OptimizeButton_Click"/>
            <Button x:Name="RestoreButton" Content="还原默认" Width="150" Height="50" Background="#2A2A2A" Foreground="#FFFFFF" FontSize="14" FontWeight="Bold" Grid.Column="2" Margin="10,0,0,0" Click="RestoreButton_Click"/>
        </Grid>
        
        <!-- 优化结果 -->
        <Border Margin="20,1250,20,40" Background="#1E1E1E" CornerRadius="8">
            <Grid Margin="20">
                <TextBlock Text="优化结果" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>
                <TextBlock x:Name="AnalyzeResultText" Text="请点击分析按钮开始分析" FontSize="14" Foreground="#888888"/>
                <TextBlock x:Name="OptimizeResultText" Text="" FontSize="14" Foreground="#00FF00" Margin="0,40,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>